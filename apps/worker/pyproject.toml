[project]
name = "rag-support-worker"
version = "0.0.0"
requires-python = "==3.13.*"
dependencies = ["rag-provider-adapters"]

[tool.uv]
package = false

[tool.uv.sources]
rag-provider-adapters = { workspace = true }
