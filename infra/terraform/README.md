# Terraform workspace

This directory is reserved for the Terraform-managed GCP environments introduced by POR-32. POR-30 adds formatting and validation commands only; it intentionally defines no cloud resources.
